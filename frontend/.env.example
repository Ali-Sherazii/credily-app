# Only used by the local Vite dev server proxy (see vite.config.js)
VITE_API_PORT=5000

# Production: your deployed backend URL (e.g. Render), used for the Socket.IO connection
VITE_SOCKET_URL=https://your-backend.onrender.com
